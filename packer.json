{
  "variables": {
    "aws_access_key_id": "{{env `AWS_ACCESS_KEY_ID`}}",
    "aws_secret_access_key": "{{env `AWS_SECRET_ACCESS_KEY`}}",
    "aws_session_token": "{{env `AWS_SESSION_TOKEN`}}",
    "time_zone": "{{env `TIME_ZONE`}}"
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key_id`}}",
      "secret_key": "{{user `aws_secret_access_key`}}",
      "token": "{{user `aws_session_token`}}",
      "region": "us-west-2",
      "source_ami": "ami-0fb71e703258ab7eb",
      "instance_type": "t3.micro",
      "ssh_username": "ec2-user",
      "associate_public_ip_address": "true",
      "ami_name": "orcas-aws-ecs-base {{timestamp}}",
      "tags": {
        "Name": "2019-11 OrcasCars.com Base Cluster Image",
        "SourceAMI": "ami-0fb71e703258ab7eb",
        "DockerVersion": "18.06.1-ce",
        "ECSAgentVersion": "1.33.0"
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "scripts-2/configure-timezone.sh",
      "environment_vars": [
        "TIME_ZONE={{user `time_zone`}}"
      ]
    },
    {
      "type": "shell",
      "script": "scripts-2/install-os-packages.sh"
    },
    {
      "type": "shell",
      "script": "scripts-2/cloud-init-options.sh"
    },
    {
      "type": "shell",
      "script": "scripts-2/cleanup.sh"
    },
    {
      "type": "file",
      "source": "files-2/firstrun.sh",
      "destination": "/home/ec2-user/firstrun.sh"
    }
  ],
  "post-processors": [
    {
      "type": "manifest",
      "output": "manifest.json",
      "strip_path": true
    }
  ]
}
